server {
    listen 80;
    server_name localhost;

    # 1. 前端靜態資源
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        expires 1h;
        add_header Cache-Control "public, no-transform";
    }

    # 2. 後端 API 反向代理 (包含 Swagger Docs)
    # 這裡統整處理所有 /api 開頭的請求
    location /api/ {
        proxy_pass http://backend:8000; # 不加斜線，完整路徑轉發
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 💡 關鍵：配合 FastAPI root_path="/api"
        proxy_set_header X-Forwarded-Prefix /api;

        # 支援 WebSocket (若未來有即時價格推播需求)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 3. 修正：讓 Swagger 能正確抓到根目錄外的 openapi.json
    # 如果你在 FastAPI 內定義 openapi_url="/openapi.json"，
    # 在 root_path="/api" 影響下，真實路徑會是 /api/openapi.json
    # 這裡做一個轉發確保萬無一失
    location = /openapi.json {
        proxy_pass http://backend:8000/api/openapi.json;
    }

    # 4. 全域效能優化
    gzip on;
    gzip_types text/plain application/javascript text/css application/json;
    gzip_min_length 1024;

    # 安全標頭
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options SAMEORIGIN;
    add_header X-XSS-Protection "1; mode=block";
}