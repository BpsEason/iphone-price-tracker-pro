server {
    listen 80;
    server_name localhost;

    # 💡 建議 1：明確定義 root，避免在每個 location 重複寫
    root /usr/share/nginx/html;
    index index.html index.htm;

    # 💡 建議 2：明確處理靜態資源 (Vite 產出的 assets)
    # 這能確保 CSS/JS 檔案被 Nginx 優先且正確地讀取
    location /assets/ {
        alias /usr/share/nginx/html/assets/;
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        access_log off;
    }

    # 前端路由處理
    location / {
        try_files $uri $uri/ /index.html; # 💡 支援 Vue Router History 模式
    }

    # 🚀 將 /api 請求轉發到後端容器
    location /api/ {
        # 💡 注意：如果你的 FastAPI 內部路由已經包含 /api，proxy_pass 結尾不要加斜槓
        # 如果 FastAPI 內部路由是從 / 開始（例如 /v1/auth），則維持現狀
        proxy_pass http://backend:8000/api/; 
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 錯誤頁面處理
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}